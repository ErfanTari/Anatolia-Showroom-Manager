# Browser application

See [the Studio guide](../README.md) for setup, the source contract and the published review link. This folder reads the local SQLite API or the derived `data/state.json` publication snapshot.
